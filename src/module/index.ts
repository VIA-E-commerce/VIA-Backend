import { AuthModule } from './auth';
import { UserModule } from './user';
import { ProductModule } from './product';
import { CartModule } from './cart';

export const modules = [AuthModule, UserModule, ProductModule, CartModule];
